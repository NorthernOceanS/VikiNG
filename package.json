{
  "name": "@bdsx/test",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bdsxPlugin": true,
  "scripts": {
    "build-plugins": "npx tsc ./plugin/index.js --outDir ./dist --allowJs --resolveJsonModule --module CommonJS",
    "build": "npm run build-plugins && tsc",
    "watch": "tsc -w",
    "prepare": "tsc || exit 0"
  },
  "devDependencies": {
    "@types/node": "^12.20.5",
    "bdsx": "file:../bdsx",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "norma-core": "^0.3.1"
  }
}
